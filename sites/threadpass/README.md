# ThreadPass

B2B one-pager for textile export compliance: EU Digital Product Passport and US UFLPA readiness, with discovery-call capture.

Pure static site: `index.html`, `styles.css`, `app.js`. No build step, no frameworks.

- **Live**: https://shubham-safaya.github.io/threadpass/

## Before launch

- [ ] Replace `FORM_ID_B` in `index.html` with a real Formspree form ID
- [ ] Swap `safayashubham@gmail.com` for a brand mailbox (in `app.js` and `index.html`)
- [ ] Add citation links for the ESPR/DPP textile timeline and CBP UFLPA statistics (TODO markers in `index.html`)
- [ ] Run a name and trademark search for "ThreadPass"

## Reskin

Brand name: `BRAND_NAME` in `app.js`. Colors: CSS variables at the top of `styles.css`.
