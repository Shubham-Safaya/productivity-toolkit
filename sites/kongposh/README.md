# Kong Posh

Pre-launch waitlist site for a GI-tagged Kashmir saffron brand with batch-level traceability.

Pure static site: `index.html`, `verify.html`, `styles.css`, `app.js`, `batches.json`. No build step, no frameworks.

- **Live**: https://shubham-safaya.github.io/kongposh/
- **Batch demo**: https://shubham-safaya.github.io/kongposh/verify.html?batch=KP-25-LTP-01

## Before launch

- [ ] Replace `FORM_ID_A` in `index.html` with a real Formspree form ID
- [ ] Swap `safayashubham@gmail.com` for a brand mailbox (in `app.js` and both HTML pages)
- [ ] Add a real family / Pampore valley photograph in the Origin section
- [ ] Insert GI authorization reference and accredited lab details
- [ ] Run a name and trademark search for "Kong Posh"

## Reskin

Brand name: `BRAND_NAME` in `app.js`. Colors: CSS variables at the top of `styles.css`.
